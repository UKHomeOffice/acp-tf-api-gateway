# lambda invocation role for authorizer
# roles for iam users? 
# if using a single bucket do we need multiple access keys for each user and also how do we authenticate to an object key? 
# could use a bucket ACL to manage access to objects in s3.
